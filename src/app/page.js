import SketchButton from "@/components/SketchButton";
import Image from "next/image";

export default function Home() {
  return (
    <div className="home-page-full w-screen h-screen md:px-[15%] lg:px-[30%] flex justify-center items-center">
      <div className="home-content-full w-full h-screen flex flex-col justify-center items-center border">
        xxx

        <SketchButton>Slime</SketchButton>

      </div>
    </div>
  );
}